{
  "name": "@rhapsodylabs/layer-generator",
  "author": "Rhapsody Labs",
  "description": "A layer generator that takes image layers input and combines them to produce an image",
  "license": "MIT",
  "version": "1.0.0",
  "main": "index.ts",
  "pkg": {
    "assets": [
      "node_modules/**/*",
      "src/**/*"
    ]
  },
  "scripts": {
    "prebuild": "rimraf ./build && tsc",
    "build": "node build/index.js",
    "test": "jest"
  },
  "dependencies": {
    "canvas": "^2.8.0",
    "sha1": "^1.1.1"
  },
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "@types/node": "^16.11.10",
    "jest": "^27.3.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^27.0.7",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.2"
  }
}
